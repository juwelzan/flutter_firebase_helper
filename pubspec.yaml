name: flutter_firebase_helper
description: "A new Flutter package."
version: 0.0.2
homepage:

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

dependencies:
  firebase_auth: ^6.5.3
  flutter:
    sdk: flutter
  google_sign_in: ^7.2.0
  firebase_core: ^4.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0


flutter:

