name: flutter_firebase_helper
description: A powerful Flutter Firebase helper package that simplifies Firebase Authentication and core services.
version: 0.0.2
homepage: https://github.com/juwelzan/flutter_firebase_helper.git

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^4.11.0
  firebase_auth: ^6.5.3
  google_sign_in: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
